# Example environment file (template)
# Copy this file to .env.production and set the values

# API URL for production deployment
# Leave empty for development (uses proxy in package.json)
REACT_APP_API_URL=https://your-api-url.com 